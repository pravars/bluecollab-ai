version: '3.8'

services:
  # Simple MongoDB setup for development
  mongodb:
    image: mongo:7.0
    container_name: dwello-mongodb
    restart: unless-stopped
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: dwello123
      MONGO_INITDB_DATABASE: dwello
    volumes:
      - mongodb_data:/data/db
      - ./database/mongodb-init.js:/docker-entrypoint-initdb.d/mongodb-init.js:ro
    command: mongod --bind_ip_all

  # MongoDB Express (Web UI)
  mongo-express:
    image: mongo-express:1.0.0
    container_name: dwello-mongo-express
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: dwello123
      ME_CONFIG_MONGODB_URL: mongodb://admin:dwello123@mongodb:27017/
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: dwello123
    depends_on:
      - mongodb

volumes:
  mongodb_data:
